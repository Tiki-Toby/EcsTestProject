using UnityEngine;

namespace GameEntities
{
    public struct ActionCameraComponent
    {
        public Vector3 FromPlayerOffset;// = new Vector3(2, 10, -5);
        public float CameraVelocity;// = 2f;
    }
}